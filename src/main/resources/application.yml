spring:
  application:
    name: repair-tracker
  
  datasource:
    url: jdbc:mysql://localhost:3306/tracker?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: mouad
  
  h2:
    console:
      enabled: false
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  
  security:
    user:
      name: admin
      password: admin

app:
  jwt:
    secret: "SuperSecureKeyThatIsAtLeast32Characters!"
    expiration: 86400000


logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.springframework.transaction: TRACE
    org.springframework.orm.jpa: TRACE


server:
  port: 8081
